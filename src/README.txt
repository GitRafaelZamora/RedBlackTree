To compile program run the following command:
	make all

Then to run the program run this command:
	./RBtree <filename>

Where <filename> is the path and name of your test file. 
I have included a few test case files that I ran for my tests
in the test folder. 